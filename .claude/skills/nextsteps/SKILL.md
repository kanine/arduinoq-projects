---
name: nextsteps
description: Get up to speed on a project by reading NEXT_STEPS.md and all files in docs/work/. Use when the user wants a project status summary, asks "where were we", or is starting a new session on an existing project.
allowed-tools: Read, Glob
---

Read `NEXT_STEPS.md` in the project root, then read every file in `docs/work/` to get the full project status. Summarise where things stand and what the immediate next task is, then await instructions.