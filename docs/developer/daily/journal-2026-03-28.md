# Journal — 2026-03-28

> Summarises commits from 2026-03-28 16:56:01 to 2026-03-28 17:28:03 / 4 commits

## Tooling & Scripts Reorganisation

(`5aee6ed`)

- Moved all shell scripts from `bash/` to `scripts/` for a cleaner repo layout
- Added `scripts/sync-skills.sh` to support the new two-way skills sync workflow
- Updated all references across `CLAUDE.md`, `GEMINI.md`, `README.md`, Copilot instructions, and app-level READMEs to point to the new `scripts/` path
- Added `.gitignore` inside `scripts/` to prevent local artefacts creeping in

## Skills Infrastructure — Claude & Agent Skill Parity

(`211f616`, `9b11bfc`)

- Added four new Claude-side skills: `journalit`, `nextsteps`, `pushit`, and `syncskills` — standardising common session workflows as invocable slash commands
- Ran the two-way sync (`sync-skills.sh`) to bring `.claude/skills/` fully in line with `.agents/skills/`, copying across all Arduino Uno Q project skills (VL53L1X integration, RouterBridge, App CLI, SQLStore, ATN-IO wiring notation, web-coder, and examples catalog)
- Backfilled the four new Claude-side skills into `.agents/skills/` so both skill trees are now identical
- Result: 46 files added, ~11,000 lines — both agent harnesses now share the same skill knowledge base

## Documentation — Sensor Reading Processing Spec

(`16c0840`)

- Authored `timeofflightwebhook/docs/handlingreadings.md` based on live analysis of 1,750 readings from the static spool baseline session
- Documented the observed noise profile: mean 65.43 mm, std dev 0.92 mm, 99.5% of readings within a 4 mm band
- Explained why the current 10% trimmed mean approach is both overkill for static conditions and actively harmful during production (clips the leading edge of the trend)
- Recommended replacing the trimmed mean with a per-batch **median**, and using **linear regression over windowed medians** for end-of-spool prediction
- Noted that at 480 ppm the high cadence makes slope estimation very stable, with prediction confidence tightening naturally as the spool approaches runout
